/**
 * Post content to Youtube.
 *
 * @param {Object} post - Post payload containing title, body, image and hashtags.
 * @returns {Promise<Object>} Result from the Youtube API.
 */
export default async function postToYoutube(post) {
  // TODO: Implement Youtube posting logic.
  // Should send `post` to Youtube and return the API response.
}
