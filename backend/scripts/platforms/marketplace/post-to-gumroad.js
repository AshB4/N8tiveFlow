/**
 * Post content to Gumroad.
 *
 * @param {Object} post - Post payload containing title, body, image and hashtags.
 * @returns {Promise<Object>} Result from the Gumroad API.
 */
export default async function postToGumroad(post) {
  // TODO: Implement Gumroad posting logic.
  // Should send `post` to Gumroad and return the API response.
}
