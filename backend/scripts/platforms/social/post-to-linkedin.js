/**
 * Post content to Linkedin.
 *
 * @param {Object} post - Post payload containing title, body, image and hashtags.
 * @returns {Promise<Object>} Result from the Linkedin API.
 */
export default async function postToLinkedin(post) {
  // TODO: Implement Linkedin posting logic.
  // Should send `post` to Linkedin and return the API response.
}
