/**
 * Post content to Tiktok.
 *
 * @param {Object} post - Post payload containing title, body, image and hashtags.
 * @returns {Promise<Object>} Result from the Tiktok API.
 */
export default async function postToTiktok(post) {
  // TODO: Implement Tiktok posting logic.
  // Should send `post` to Tiktok and return the API response.
}
